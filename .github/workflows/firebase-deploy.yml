# GitHub Action: Deploy Astro site to Firebase Hosting
# Place this file in .github/workflows/firebase-deploy.yml

name: Deploy to Firebase Hosting

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        run: npm ci

      - name: Run prebuild script
        run: npm run prebuild

      - name: Build Astro site
        run: npm run build

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}'
          channelId: live
          projectId: ${{ secrets.FIREBASE_PROJECT_ID }}

      - name: Notify PubSubHubbub/WebSub hub
        run: |
          curl -X POST https://pubsubhubbub.appspot.com/publish \
            -d "hub.mode=publish" \
            -d "hub.url=https://hellostu.xyz/rss-feed.xml"
        env:
          FEED_URL: https://hellostu.xyz/rss-feed.xml
